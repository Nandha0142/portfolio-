Portfolio (Multi-file) — Nandhish Srinivasan
Dark luxury portfolio with a Three.js 3D hero + clear contact CTAs.

Files
index.html
styles.css
script.js (Three.js via CDN module import)
Run (recommended)
Because this uses ES modules, run it with a local server.

Option A: Node (if installed)

Bash

1
npx serve .
Option B: Python (if installed)

Bash

1
python -m http.server 8000
